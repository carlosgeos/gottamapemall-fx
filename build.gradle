group 'be.ac.ulb.infof307.g07'
version '1.0-SNAPSHOT'

allprojects {
    repositories {
        mavenCentral()
        flatDir {
            dirs 'lib'
        }
    }
}

subprojects { }

project(':client') {
    // mainClassName = "be.ac.ulb.infof307.g07.client.MainGUI"

    // sourceCompatibility = '1.8'

    apply plugin: 'java'
    apply plugin: 'application'
    apply plugin: 'idea'

    configurations {
        doc {
            transitive false
        }
    }

    dependencies {
        // compile name: 'GMapsFX-2.11.0-SNAPSHOT'
        compile group: 'com.lynden', name: 'GMapsFX', version: '2.11.0'
        testCompile group: 'junit', name: 'junit', version: '4.11'
        compile 'com.jfoenix:jfoenix:1.2.0'
    }

    javadoc {
        classpath += configurations.compile
        source += sourceSets.main.allJava
        destinationDir = file("${projectDir}/doc")
    }
}
